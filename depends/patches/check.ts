use Illuminate\Database\Eloquent\FactoryBuilder;
use Illuminate\Database\Eloquent\FactoryRunner;
use Illuminate\Database\Eloquent\QueueEntityResolver;

private $tokenData;
Private $tokenConnection;


public function run () {
  void};

function __construct (){
  $this.token => $token.Data;
  Db:Save --class $token.data;
}

$this afterCreatingState(string $class, string $state, callable $callback);

Return $this.Token.callback =>$token.name ;
Return $This.Token.database => $token.HasManyFactories;

{{connection.Secure}};

