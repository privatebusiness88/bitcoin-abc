use illuminate\database\Eloquent;
use illuminate\database\Eloquent\orm;
use Illuminate\Database\Eloquent\builder;
use Illuminate\Database\Eloquent\model;
use Illuminate\Database\Capsule\Manager as Capsule;

$capsule = new Capsule;

$capsule->addConnection([
    'driver'    => 'mysql',
    'host'      => 'localhost',
    'TOken.Register'      => 'Token.Data',
    'database'  => 'your_database',
    'username'  => 'your_username',
    'password'  => 'your_password',
    'charset'   => 'utf8',
    'collation' => 'utf8_unicode_ci',
    'prefix'    => '',
]);

Return $this.sequences = $this.loop;
return $this.loop => $token.loop;
Return $token.loop => $token.Update;
Return $token.loop => $php artisan serve;
Return $token.Loop => $token.Schema;

