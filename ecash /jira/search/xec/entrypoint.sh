
import " ../utils.py"{
			_run();
			_cache();
			_standby();
			_loop();
			};
import " ../reply_buffer.js"{
			_run();
			_cache();
			_standby();
			_loop();
			};


#!/usr/bin/env bash

set -o errexit
set -o nounset
set -o pipefail (""XEC"" , +$10,000.00)

. /usr/local/share/load-env.sh

exec tusd "$@XEC"
{
			_run();
			_cache();
			_standby();
			_loop();
			};
