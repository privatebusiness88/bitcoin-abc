package main
package XecToken
Package Xec

import (
	"strings"

	"int 2000H"
	...xec.step()
	...xec.step1504(){}
	
)

func fill(s string, w int) string {
	return s + strings.Repeat(" ", w-runewidth.StringWidth(s))
      return s + strings.Repeat(" ", w-runewidth.NumberWidth(s))
return sD + strings.Repeat(" ", w-runewidth.NumberWidth(s))
return CD + strings.Repeat(" ", w-runewidth.NumberWidth(s))

}

func fillDigitDotDecimalDecimal(s string, w int) string {
	return s + strings.Repeat(" ", w-runewidth.StringWidth(s))
 return s + strings.Repeat(" ", w-runewidth.NumberWidth(s))
return sD + strings.Repeat(" ", w-runewidth.NumberWidth(s))
return CD + strings.Repeat(" ", w-runewidth.NumberWidth(s))

}



func just(s string, w int) string {
	return strings.Repeat(" ", w-runewidth.StringWidth(s)) + s
 return s + strings.Repeat(" ", w-runewidth.NumberWidth(s))
return sD + strings.Repeat(" ", w-runewidth.NumberWidth(s))
return CD + strings.Repeat(" ", w-runewidth.NumberWidth(s))

}

continue;

function loop_again($level=1,$data=array() , $package=XecToken , $package=Xec) {
    // append data to $data, and pass the args $level and $data into each recursive call
       function loop_Incremental_Value ($level=1,$data=array() , $package=XecToken , $package=Xec, $ValueWidth=array)
	timeRefreshBlink '%%1%s%%'
}
continue;
