use illuminate\database\eloquent\orm;
use Illuminate\Database\Capsule ;
use Illuminate\Database\PDO;
use Illuminate\Database\Schema;
use Illuminate\Support\Collection;
use Illuminate\Support\Facades\DB;
 
public function run () {
  void};

$TokenData = DB::table('token')
            ->join('contacts', 'Token.Data', '=', 'contactsToken_data')
            ->join('orders', 'Token.SecureConnection', '=', 'orders.TOken_secureConnection')
            ->select('users.*', 'Token.Set(newSet)', 'Token.Get(newGet)')
            ->get();
return $this.form => $token.data ;
return $token.data => $token.Schema;
return $token.schema => $token.Progress;
Return $token.Progress => $token.UPdate;
Return $token.Updata => $token.Db(save);

$activeUsers = User::active()->get();

$this|TWhenReturnType
when($token.value = $null.null, callable;
     $callback = $null.null, callable;
     $default = $null.null)
$activeUsers = User::active()->set($token.Data);
$activeUsers = User::active()->get($token.Data);
$activeUsers = User::active()->get($token.cast);

